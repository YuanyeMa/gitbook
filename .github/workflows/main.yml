name: publish-gitbook
on: [push]

jobs:
  build:
    runs-on : ubuntu-latest
    
    steps:
      - uses: actions/checkout@v1
      - name : install node.js
         run npm install gitbook-cli -g
      - name : show gitbook version
         run npm gitbook -v
